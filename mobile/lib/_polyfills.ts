/**
 * Polyfill anchor.
 *
 * Keep this file empty unless we need to add Web API polyfills (e.g. for
 * the WebSocket fallback, structuredClone, etc.) before the app boots.
 * Imported once from app/_layout.tsx.
 */

export {};
